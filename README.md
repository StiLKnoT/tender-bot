# 🤖 Tender Aggregator Bot (Xarid.uz & IT-Market)

Телеграм-бот для автоматического мониторинга тендерных площадок Узбекистана.
Бот парсит новые лоты, фильтрует их по ключевым словам и цене, сохраняет в Google Таблицу и отправляет уведомления в Telegram канал.

## 🚀 Возможности
- **Источники:** Xarid.uz (аукционы) и IT-Market.uz (заказы).
- **Фильтрация:** По ключевым словам и минимальной цене.
- **Google Sheets:** Автоматическая выгрузка всех найденных лотов в таблицу.
- **Telegram:** Красивые карточки с кнопками (Лайк/Пропустить).
- **База данных:** PostgreSQL (хранение истории и избранного).
- **Асинхронность:** Одновременная работа парсера и бота.

## 🛠 Требования
- Python 3.8+
- PostgreSQL (сервер базы данных)
- Аккаунт Google Cloud (для API Таблиц)

## 📦 Установка

### 1. Клонирование и подготовка
Создайте папку проекта и поместите туда файлы скрипта.

### 2. Виртуальное окружение
Рекомендуется использовать `venv` для изоляции библиотек.

**Windows:**
```bash
python -m venv venv
.\venv\Scripts\activate

**macOS / Linux::**
python3 -m venv venv
source venv/bin/activate

**Установка зависимостей:**
pip install -r requirements.txt

**Установка браузера (для Playwright):**
playwright install chromium

**База данных PostgreSQL:**
CREATE DATABASE tender_bot_db;